"""
tools — Diagnostic and maintenance utilities.

Modules:
    diag_tcp                  — Raw TCP protobuf diagnostic (send/receive/dump)
    sync_check                — Validate Python ↔ PLC registry synchronization
    fix_varmon_private        — Remove inaccessible variables from registry
    add_varmonlib_to_configs  — Patch PLC project files for VarMonLib
    update_varmon             — Update VarMonLib source in PLC project
"""
